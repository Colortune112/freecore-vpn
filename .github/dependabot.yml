# FreeCore VPN — Dependabot disabled.
#
# Hiddify-upstream имел gitsubmodule-обновления — для нас это шум: каждый
# тик коммита в hiddify-core порождает PR. Если позже понадобится
# контролируемое обновление — добавить package-ecosystem: "pub" /
# "gradle" / "github-actions" вручную.
version: 2
updates: []
